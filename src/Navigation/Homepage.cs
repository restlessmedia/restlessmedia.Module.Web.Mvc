namespace restlessmedia.Module.Web.Navigation
{
  public class Homepage : NavItem
  {
    protected Homepage(string title, string url, string controller, string action)
      : base(title, url, controller, action)
    {
    }
  }
}